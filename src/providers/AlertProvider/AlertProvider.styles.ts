import { styled } from "@mui/material";

const AlertContainer = styled("div")({
  position: "fixed",
  width: "40%",
  right: "0.6rem",
  bottom: "0.6rem",
  zIndex: "10",
});

export { AlertContainer };
